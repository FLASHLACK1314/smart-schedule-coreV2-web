# 前端开发任务清单

> 本文档列出前端可以立即实现的功能模块，基于后端 API 完成情况分析
>
> **生成日期**: 2026-02-11
> **后端 API 完成度**: 第一、二阶段 100% 完成（9个控制器）

---

## 📊 概览

### ✅ 已对接模块（5个）
- 用户认证系统
- 教学楼管理系统
- 教室管理系统
- 个人中心系统
- 首页导航系统

### 🟢 可立即对接（4个）
- **教师管理** - UI 已完成，后端已完成
- **学院管理** - UI 已完成，后端已完成
- **专业管理** - UI 已完成，后端已完成
- **班级管理** - UI 已完成，后端已完成

### 🔵 新增页面（2个）
- **课程管理** - 后端已完成，前端需新建页面
- **课程类型管理** - 后端已完成，前端需新建页面

### 🟡 待补充后端（2个）
- 学生管理 - 后端仅 add 完成
- 教务管理 - 后端未实现

---

## 🎯 任务一：教师管理对接（优先级：🔥 最高）

### 后端控制器
**TeacherController**

### 前端文件
- **页面**: `src/views/TeacherManagementView.vue`（已存在）
- **API**: `src/api/teacher.ts`（需创建）

### UI 改造要点

1. **表单字段**:
   - 教师编号（唯一）
   - 教师姓名
   - 职称（下拉选择）
   - 所属学院（下拉选择）
   - 密码（新增时必填，编辑时可选）
   - 每周最大课时（数字输入）
   - 时间偏好（可简化为可选配置）
   - 启用状态（开关）

2. **搜索功能**:
   - 教师姓名（模糊）
   - 教师工号（模糊）
   - 所属学院（下拉筛选）

3. **数据关联**:
   - 学院选项来源：调用 `DepartmentController`

---

## 🎯 任务二：学院管理对接（优先级：🔥 最高）

### 后端控制器
**DepartmentController**

### 前端文件
- **页面**: `src/views/CollegeManagementView.vue`（已存在）
- **API**: `src/api/department.ts`（需创建）

### UI 改造要点

1. **表单字段**:
   - 学院名称（唯一）

2. **搜索功能**:
   - 学院名称（模糊）

3. **删除检查**:
   - 删除前会检查是否有专业引用

---

## 🎯 任务三：专业管理对接（优先级：🔥 最高）

### 后端控制器
**MajorController**

### 前端文件
- **页面**: `src/views/MajorManagementView.vue`（已存在）
- **API**: `src/api/major.ts`（需创建）

### UI 改造要点

1. **表单字段**:
   - 专业编号（唯一）
   - 专业名称
   - 所属学院（下拉选择）

2. **搜索功能**:
   - 专业名称（模糊）
   - 专业编号（模糊）
   - 所属学院（下拉筛选）

3. **数据关联**:
   - 学院选项来源：调用 `DepartmentController`

4. **删除检查**:
   - 删除前会检查是否有班级引用

---

## 🎯 任务四：班级管理对接（优先级：🔥 最高）

### 后端控制器
**ClassController**

### 前端文件
- **页面**: `src/views/ClassManagementView.vue`（需新建）
- **API**: `src/api/class.ts`（需创建）
- **路由**: 需在 `router/index.ts` 中添加

### UI 改造要点

1. **表单字段**:
   - 班级名称（同一专业下唯一）
   - 所属专业（下拉选择）
   - 所属学院（自动关联显示，不可编辑）

2. **搜索功能**:
   - 班级名称（模糊）
   - 所属专业（下拉筛选）
   - 所属学院（下拉筛选）

3. **数据关联**:
   - 专业选项来源：调用 `MajorController`
   - 根据选择的专业自动显示所属学院

4. **删除检查**:
   - 删除前会检查是否有学生引用

---

## 🎯 任务五：课程管理（新增页面）

### 后端控制器
**CourseController**

### 前端文件
- **页面**: `src/views/CourseManagementView.vue`（需新建）
- **API**: `src/api/course.ts`（需创建）
- **路由**: 需在 `router/index.ts` 中添加

### 页面实现要点

1. **表单字段**:
   - 课程编号（唯一）
   - 课程名称
   - 课程类型（下拉选择）
   - 课程学分（数字，支持小数，如 2.5）

2. **搜索功能**:
   - 课程名称（模糊）
   - 课程编号（模糊）
   - 课程类型（下拉筛选）

3. **数据关联**:
   - 课程类型选项来源：调用 `CourseTypeController`

4. **删除检查**:
   - 删除前会检查是否有教学班或课程资格引用

---

## 🎯 任务六：课程类型管理（新增页面）

### 后端控制器
**CourseTypeController**

### 前端文件
- **页面**: `src/views/CourseTypeManagementView.vue`（需新建）
- **API**: `src/api/courseType.ts`（需创建）
- **路由**: 需在 `router/index.ts` 中添加

### 页面实现要点

1. **表单字段**:
   - 类型名称（唯一，如：必修课、选修课、实践课）

2. **搜索功能**:
   - 类型名称（模糊）

---

## 🔧 数据源依赖关系

### 学院（Department）
**被以下模块依赖**:
- 教师管理（下拉选择）
- 专业管理（下拉选择）
- 教务管理（下拉选择）

**获取方式**: `DepartmentController`

---

### 专业（Major）
**被以下模块依赖**:
- 班级管理（下拉选择）

**获取方式**: `MajorController`
**筛选参数**: `departmentUuid`（按学院筛选）

---

### 课程类型（CourseType）
**被以下模块依赖**:
- 课程管理（下拉选择）

**获取方式**: `CourseTypeController`

---

## 📝 TypeScript 类型定义

### 需要在 `src/api/types.ts` 中补充的类型

```typescript
// 教师信息
export interface TeacherInfoDTO {
  teacher_uuid: string
  teacher_num: string
  teacher_name: string
  title: string
  department_uuid: string
  department_name?: string
  max_hours_per_week: number
  like_time: string
  is_active: boolean
}

// 学院信息
export interface DepartmentInfoDTO {
  department_uuid: string
  department_name: string
}

// 专业信息
export interface MajorInfoDTO {
  major_uuid: string
  major_num: string
  major_name: string
  department_uuid: string
  department_name?: string
}

// 班级信息
export interface ClassInfoDTO {
  class_uuid: string
  class_name: string
  major_uuid: string
  major_name?: string
  department_uuid?: string
  department_name?: string
}

// 课程信息
export interface CourseInfoDTO {
  course_uuid: string
  course_num: string
  course_name: string
  course_type_uuid: string
  course_type_name?: string
  course_credit: number
}

// 课程类型信息
export interface CourseTypeInfoDTO {
  course_type_uuid: string
  type_name: string
}
```

---

## 🚀 开发顺序建议

### 第一批：立即对接（UI 已存在）
1. ✅ **学院管理** - 最简单，无依赖
2. ✅ **专业管理** - 依赖学院
3. ✅ **教师管理** - 依赖学院
4. ✅ **班级管理** - 依赖专业

### 第二批：新增页面（后端已完成）
5. ✅ **课程类型管理** - 无依赖
6. ✅ **课程管理** - 依赖课程类型

---

## ⚠️ 注意事项

### 1. 数据验证
- 教师工号、专业编号、课程编号等唯一性字段由后端校验
- 前端只需进行基本的非空验证

### 2. 删除操作
- 后端会检查引用关系，有引用的无法删除
- 前端需要捕获错误并显示友好提示

### 3. 时间偏好字段
- `like_time` 是 JSONB 格式，如 `{"1": [3,4]}` 表示周一第3-4节
- 建议第一版简化为可选配置，不实现复杂的可视化编辑

### 4. 学分字段
- `course_credit` 使用 `BigDecimal` 类型
- 前端使用 `number` 类型，注意精度处理

### 5. 关联数据展示
- 列表页显示关联数据名称（如学院名称、专业名称）
- 这些字段后端已通过关联查询提供

### 6. 下拉选择器
- 使用 MCP 工具获取对应的 Controller 接口信息
- 下拉数据通常需要调用 `getPage` 接口获取全量数据（设置 size=100）

---

## 📊 开发进度追踪

### API 封装
- [ ] `src/api/teacher.ts`
- [ ] `src/api/department.ts`
- [ ] `src/api/major.ts`
- [ ] `src/api/class.ts`
- [ ] `src/api/course.ts`
- [ ] `src/api/courseType.ts`

### 页面对接
- [ ] 教师管理页面对接
- [ ] 学院管理页面对接
- [ ] 专业管理页面对接
- [ ] 班级管理页面对接
- [ ] 课程类型管理页面
- [ ] 课程管理页面

### 路由配置
- [ ] 添加班级管理路由
- [ ] 添加课程管理路由
- [ ] 添加课程类型管理路由

---

## 📖 参考文档

- **后端 API 文档**: `API_IMPLEMENTATION_ORDER.md`
- **前端功能文档**: `前端已完成功能文档.md`
- **类型定义**: `src/api/types.ts`
- **路由配置**: `src/router/index.ts`

---

**文档版本**: v2.0
**最后更新**: 2026-02-11
**维护者**: 开发团队