# 智能排课系统 V2.0 - 已完成功能文档

> 本文档记录了项目截止 **2026-02-11** 的所有已实现功能和接口对接情况

## 📋 项目概览

| 项目信息 | 详情 |
|---------|------|
| **项目名称** | 智能排课系统 (Smart Schedule System V2.0) |
| **技术栈** | Vue 3.5 + TypeScript 5.9 + Vite 7 + Pinia |
| **设计风格** | 深色主题，毛玻璃效果，渐变色系（青蓝 #00d4ff → 紫色 #7c3aed） |
| **UI 组件** | 无第三方组件库，纯原生 CSS 和自定义组件 |

---

## 🎯 已完成功能模块

### 1. 用户认证系统 ✅

#### 登录页面
**文件**: [LoginView.vue](src/views/LoginView.vue)

**功能特性**:
- 支持 4 种用户类型登录：学生、教师、教务管理员、系统管理员
- 卡片式用户类型选择器
- 表单验证（用户名、密码）
- 加载状态与错误提示
- 登录成功后根据用户类型跳转

**对接接口**:
```
POST /v1/auth/login
```

**请求参数**:
```typescript
interface LoginVO {
  user_type: string    // STUDENT | TEACHER | ACADEMIC_ADMIN | SYSTEM_ADMIN
  user_name: string
  password: string
}
```

**响应数据**:
```typescript
interface GetUserLoginDTO {
  user_type: string
  token: string
  student_info?: StudentUserInfoDTO
  teacher_info?: TeacherUserInfoDTO
  academic_admin_info?: AcademicAdminUserInfoDTO
  system_admin_info?: SystemAdminUserInfoDTO
}
```

---

#### 修改密码页面
**文件**: [ChangePasswordView.vue](src/views/ChangePasswordView.vue)

**功能特性**:
- 新密码输入（6 位以上）
- 确认密码验证
- 系统管理员不支持修改密码

**对接接口**:
```
POST /v1/auth/change-password
```

---

### 2. 教学楼管理系统 ✅

#### 教学楼管理页面
**文件**: [BuildingManagementView.vue](src/views/BuildingManagementView.vue)

**功能特性**:
- 分页查询教学楼列表
- 智能搜索（包含字母/数字优先搜索编号，纯中文搜索名称）
- 添加教学楼（编号、名称）
- 编辑教学楼（编号不可修改）
- 删除教学楼
- 数据验证和错误提示

**数据结构**:
```typescript
interface BuildingInfoDTO {
  building_uuid: string
  building_num: string      // 教学楼编号
  building_name: string     // 教学楼名称
}
```

**对接接口**:
| 方法 | URL | 用途 |
|------|-----|------|
| GET | `/v1/building/getPage?page={page}&size={size}` | 分页查询教学楼 |
| GET | `/v1/building/get?building_uuid={uuid}` | 获取单个教学楼 |
| POST | `/v1/building/add` | 新增教学楼 |
| PUT | `/v1/building/update` | 更新教学楼 |
| DELETE | `/v1/building/delete?building_uuid={uuid}` | 删除教学楼 |

---

### 3. 教室管理系统 ✅

#### 教室管理页面
**文件**: [ClassroomManagementView.vue](src/views/ClassroomManagementView.vue)

**功能特性**:
- 按教学楼筛选教室
- 搜索教室名称、教学楼、类型
- 添加教室（选择教学楼、教室名称、容量、教室类型）
- 编辑教室
- 删除教室
- 教室类型动态加载

**数据结构**:
```typescript
interface ClassroomInfoDTO {
  classroom_uuid: string
  building_name: string
  classroom_name: string
  capacity: number
  type_name: string
  type_description: string
}
```

**对接接口**:
| 方法 | URL | 用途 |
|------|-----|------|
| GET | `/v1/classroom/getPage?page={page}&size={size}` | 分页查询教室 |
| GET | `/v1/classroom/get?classroom_uuid={uuid}` | 获取单个教室 |
| POST | `/v1/classroom/add` | 新增教室 |
| PUT | `/v1/classroom/update` | 更新教室 |
| DELETE | `/v1/classroom/delete?classroom_uuid={uuid}` | 删除教室 |
| GET | `/v1/classroomType/getPage` | 获取教室类型列表 |

---

### 4. 个人中心系统 ✅

#### 个人信息页面
**文件**: [ProfileView.vue](src/views/ProfileView.vue)

**功能特性**:
- 根据用户类型动态显示不同信息字段
- **学生信息**: 学号、姓名、行政班级
- **教师信息**: 教师编号、姓名、职称、学院、每周最大课时、状态
- **教务管理员信息**: 教务编号、姓名、学院
- **系统管理员信息**: 用户名
- 修改密码入口（系统管理员不支持）

---

### 5. 首页导航系统 ✅

#### 首页
**文件**: [HomeView.vue](src/views/HomeView.vue)

**功能特性**:
- 用户信息展示（头像、姓名、用户类型）
- 根据用户类型动态显示功能模块
- 退出登录功能

**功能模块映射**:

| 功能名称 | 路由路径 | 可见用户 |
|---------|---------|----------|
| 智能排课 | /schedule | 学生、教师 |
| 课程管理 | /course-management | 教务管理员、系统管理员 |
| 教师管理 | /teacher-management | 教务管理员、系统管理员 |
| 教学楼管理 | /building-management | 教务管理员、系统管理员 |
| 教室管理 | /classroom-management | 教务管理员、系统管理员 |
| 班级管理 | /class-management | 教务管理员、系统管理员 |
| 学生管理 | /student-management | 系统管理员 |
| 学院管理 | /college-management | 教务管理员、系统管理员 |
| 专业管理 | /major-management | 教务管理员、系统管理员 |
| 教务管理 | /academic-management | 系统管理员 |
| 我的课表 | /my-schedule | 学生、教师 |

---

### 6. 全局消息提示系统 ✅

#### MessageToast 组件
**文件**: [MessageToast.vue](src/components/MessageToast.vue)

**功能特性**:
- 4 种消息类型：成功 ✅、错误 ❌、警告 ⚠️、信息 ℹ️
- 从右侧滑入动画
- 支持堆叠显示
- 自动关闭（默认 3 秒）或手动关闭
- 深色主题，毛玻璃背景

#### useMessage Composable
**文件**: [useMessage.ts](src/composables/useMessage.ts)

**使用方法**:
```typescript
import { useMessage } from '@/composables/useMessage'

const { success, error, warning, info } = useMessage()

// 成功消息（绿色）
success('操作成功！')

// 错误消息（红色）
error('操作失败，请重试')

// 警告消息（橙色）
warning('请注意检查输入')

// 信息消息（蓝色）
info('数据加载中...')

// 自定义显示时长（毫秒）
success('保存成功', 5000)

// 不自动关闭（需要手动关闭）
error('严重错误', 0)
```

---

## 🔧 基础架构

### 路由配置
**文件**: [router/index.ts](src/router/index.ts)

**已实现路由**:

| 路径 | 名称 | 组件 | 需要登录 | 实现状态 |
|------|------|------|----------|----------|
| /login | Login | LoginView.vue | 否 | 已实现 |
| / | Home | HomeView.vue | 是 | 已实现 |
| /profile | Profile | ProfileView.vue | 是 | 已实现 |
| /change-password | ChangePassword | ChangePasswordView.vue | 是 | 已实现 |
| /building-management | BuildingManagement | BuildingManagementView.vue | 是 | 已实现 |
| /classroom-management | ClassroomManagement | ClassroomManagementView.vue | 是 | 已实现 |
| /teacher-management | TeacherManagement | TeacherManagementView.vue | 是 | 待API对接 |
| /student-management | StudentManagement | StudentManagementView.vue | 是 | 待API对接 |
| /academic-management | AcademicManagement | AcademicManagementView.vue | 是 | 待API对接 |
| /college-management | CollegeManagement | CollegeManagementView.vue | 是 | 待API对接 |
| /major-management | MajorManagement | MajorManagementView.vue | 是 | 待API对接 |

**路由守卫**: 检查 token，未登录自动跳转到登录页

---

### API 接口封装

#### 基础配置
- **Base URL**: `import.meta.env.VITE_API_BASE_URL`
- **超时时间**: 10 秒
- **Content-Type**: application/json
- **认证方式**: Authorization header

#### 已封装 API 模块

**认证相关** ([api/auth.ts](src/api/auth.ts)):
```typescript
login(data: LoginVO)          // POST /v1/auth/login
logout()                       // DELETE /v1/auth/logout
changePassword(data: any)     // POST /v1/auth/change-password
```

**教学楼相关** ([api/building.ts](src/api/building.ts)):
```typescript
getBuildingPage(page, size)           // GET /v1/building/getPage
getBuildingInfo(building_uuid)        // GET /v1/building/get
addBuilding(data)                     // POST /v1/building/add
updateBuilding(data)                  // PUT /v1/building/update
deleteBuilding(building_uuid)         // DELETE /v1/building/delete
```

**教室相关** ([api/classroom.ts](src/api/classroom.ts)):
```typescript
getClassroomInfo(classroom_uuid)      // GET /v1/classroom/get
addClassroom(data)                    // POST /v1/classroom/add
getClassroomPage(page, size)          // GET /v1/classroom/getPage
updateClassroom(data)                 // PUT /v1/classroom/update
deleteClassroom(classroom_uuid)       // DELETE /v1/classroom/delete
```

**教室类型相关** ([api/classroomType.ts](src/api/classroomType.ts)):
```typescript
getClassroomTypePage(page, size)      // GET /v1/classroomType/getPage
getClassroomType(type_uuid)           // GET /v1/classroomType/get
```

---

### 状态管理 (Pinia)

#### userStore
**文件**: [stores/user.ts](src/stores/user.ts)

**状态**:
```typescript
token: string              // 登录令牌
userInfo: any             // 用户信息（根据用户类型存储）
userType: UserType        // 用户类型
isLoggedIn: boolean       // 是否已登录
```

**方法**:
```typescript
login(loginData: LoginVO)  // 登录
logout()                   // 登出
```

---

### 工具函数

#### storage 封装
**文件**: [utils/storage.ts](src/utils/storage.ts)

**功能**: localStorage 封装
- `setToken()` / `getToken()` / `removeToken()`
- `setUserInfo()` / `getUserInfo()` / `removeUserInfo()`
- `setUserType()` / `getUserType()` / `removeUserType()`
- `clearAuth()`: 清除所有登录信息

---

## 📦 TypeScript 类型定义

**文件**: [api/types.ts](src/api/types.ts)

**核心类型**:

```typescript
// 通用响应结构
interface BaseResponse<T> {
  output: 'Success' | 'OperationFailed'
  code: number
  message: string
  data: T
  error_message?: string
}

// 用户类型枚举
enum UserType {
  STUDENT = 'STUDENT'
  TEACHER = 'TEACHER'
  ACADEMIC_ADMIN = 'ACADEMIC_ADMIN'
  SYSTEM_ADMIN = 'SYSTEM_ADMIN'
}

// 分页查询响应
interface PageDTO<T> {
  total: number
  page: number
  size: number
  records: T[]
}

// 教学楼信息
interface BuildingInfoDTO {
  building_uuid: string
  building_num: string
  building_name: string
}

// 教室信息
interface ClassroomInfoDTO {
  classroom_uuid: string
  building_name: string
  classroom_name: string
  capacity: number
  type_name: string
  type_description: string
}

// 教室类型信息
interface ClassroomTypeInfoDTO {
  type_uuid: string
  type_name: string
  type_description: string
}
```

---

## 🎨 设计规范

### 深色主题配色方案
- **主背景渐变**: `#0f0f1a` → `#1a1a2e` → `#16213e`
- **主题色渐变**: `#00d4ff`（青蓝）→ `#7c3aed`（紫色）
- **文字颜色**:
  - 主标题: `#ffffff`
  - 副标题/描述: `#a0aec0`
- **卡片背景**: `rgba(30, 30, 50, 0.8)` 到 `rgba(40, 40, 70, 0.8)`
- **边框**: `rgba(255, 255, 255, 0.1)`

### 视觉特效
- **毛玻璃效果**: `backdrop-filter: blur(10px)`
- **发光效果**: 使用青蓝色的阴影 `rgba(0, 212, 255, 0.2)`
- **渐变文字**: 使用 `-webkit-background-clip: text`

### 卡片设计
- **圆角**: 16px - 20px
- **内边距**: 2rem
- **悬停效果**:
  - 上移 8px
  - 顶部显示渐变线条
  - 发光阴影

### 动画效果
- **浮动动画**: 3 秒循环的上下浮动
- **过渡时长**: 0.3s
- **缓动函数**: `cubic-bezier(0.4, 0, 0.2, 1)`

---

## 🚧 待实现功能

### 1. 教师管理（UI 已完成）
**文件**: [TeacherManagementView.vue](src/views/TeacherManagementView.vue)

**功能**:
- 搜索教师姓名、工号、职称
- 添加/编辑教师
- 删除教师

**状态**: ⏳ 需要对接后端 API

---

### 2. 学生管理（UI 已完成，含模拟数据）
**文件**: [StudentManagementView.vue](src/views/StudentManagementView.vue)

**功能**:
- 搜索学生姓名、学号、班级
- 添加/编辑学生
- 删除学生
- 状态管理：在读、休学、毕业

**状态**: ⏳ 需要对接后端 API

---

### 3. 教务管理（UI 已完成，含模拟数据）
**文件**: [AcademicManagementView.vue](src/views/AcademicManagementView.vue)

**功能**:
- 搜索姓名、工号、部门
- 添加/编辑教务人员
- 删除教务人员
- 权限管理

**状态**: ⏳ 需要对接后端 API

---

### 4. 学院管理（UI 已完成）
**文件**: [CollegeManagementView.vue](src/views/CollegeManagementView.vue)

**功能**:
- 搜索学院名称、编码、院长
- 添加/编辑学院
- 删除学院

**状态**: ⏳ 需要对接后端 API

---

### 5. 专业管理（UI 已完成，含模拟数据）
**文件**: [MajorManagementView.vue](src/views/MajorManagementView.vue)

**功能**:
- 搜索专业名称、编码、所属学院
- 添加/编辑专业
- 删除专业

**状态**: ⏳ 需要对接后端 API

---

### 6. 核心业务功能（未实现）

- 智能排课功能
- 课程管理功能
- 班级管理功能
- 我的课表功能

---

## 📊 开发进度统计

| 模块 | 页面数 | 已完成接口 | 待对接接口 | 完成度 |
|------|--------|-----------|-----------|--------|
| 用户认证 | 2 | 2/2 | 0 | 100% |
| 教学楼管理 | 1 | 5/5 | 0 | 100% |
| 教室管理 | 1 | 6/6 | 0 | 100% |
| 个人中心 | 2 | 1/1 | 0 | 100% |
| 教师管理 | 1 | 0/5 | 5 | 0% |
| 学生管理 | 1 | 0/5 | 5 | 0% |
| 教务管理 | 1 | 0/5 | 5 | 0% |
| 学院管理 | 1 | 0/5 | 5 | 0% |
| 专业管理 | 1 | 0/5 | 5 | 0% |
| **总计** | **12** | **14/39** | **25** | **36%** |

---

## 📝 开发规范

### 组件开发规范
- 使用 `<script setup lang="ts">` 语法
- 使用 `<style scoped>` 避免样式污染
- 为 props 定义 TypeScript 接口
- 组件文件使用 PascalCase（如 `HomePage.vue`）
- 响应式设计：所有页面必须适配移动端

### API 开发规范
- 所有接口调用都使用封装好的 API 函数
- 统一错误处理
- 使用 `useMessage` 替代 `alert()`
- 加载状态使用 loading 变量控制

### 代码质量
- 每次提交前运行类型检查：`npm run type-check`
- 确保构建成功：`npm run build`
- 遵循 TypeScript 严格模式
- 禁止使用 `any` 类型（特殊情况需要注释说明）

---

## 🔗 相关文件

| 文件 | 说明 |
|------|------|
| [CLAUDE.md](CLAUDE.md) | 项目开发指南 |
| [package.json](package.json) | 项目依赖配置 |
| [vite.config.ts](vite.config.ts) | Vite 构建配置 |
| [tsconfig.json](tsconfig.json) | TypeScript 配置 |
| [src/main.ts](src/main.ts) | 应用入口 |
| [src/App.vue](src/App.vue) | 根组件 |

---

## 📅 更新记录

| 日期 | 版本 | 更新内容 |
|------|------|----------|
| 2026-02-11 | v2.0.0 | 创建本文档，记录已完成的功能 |

---

**文档维护**: 本文档应随着项目进展持续更新
**最后更新**: 2026-02-11
