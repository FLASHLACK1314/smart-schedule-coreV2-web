# 前端接入自动化排课功能文档

## Context

后端已实现完整的遗传算法自动排课功能，包括服务层和控制器层。前端需要对接5个API接口以实现自动排课的前端界面和交互功能。

## 接口概览

| 接口路径 | 方法 | 描述 | 权限 |
|---------|------|------|------|
| `/v1/auto-schedule/execute` | POST | 执行自动排课 | 系统管理员、教务管理员 |
| `/v1/auto-schedule/save-preview` | POST | 保存预览方案 | 系统管理员、教务管理员 |
| `/v1/auto-schedule/confirm` | POST | 确认排课方案 | 系统管理员、教务管理员 |
| `/v1/auto-schedule/clear-preview` | DELETE | 清除预览方案 | 系统管理员、教务管理员 |
| `/v1/auto-schedule/statistics` | GET | 获取排课统计信息 | 系统管理员、教务管理员 |

## 接口详情

### 1. 执行自动排课

**接口路径**: `POST /v1/auto-schedule/execute`

**请求头**:
```
Authorization: Bearer {token}
```

**请求体** (AutoScheduleVO):
```json
{
  "semester_uuid": "string",

  "course_class_mapping": {"string": ["string"]},

  "teacher_assignment": {"string": "string"},
  "teacher_selection_strategy": "balanced",

  "building_uuids": ["string"],

  "schedule_mode": 0,
  "overwrite": false,

  "population_size": 100,
  "max_generations": 500,
  "crossover_rate": 0.8,
  "mutation_rate": 0.2,
  "elite_size": 10
}
```

**排课模式说明**：

按行政班级排课模式：
- 从课程和行政班角度进行排课
- 前端只需提供"课程UUID → 行政班列表"的映射
- 后端自动：
  1. 智能匹配或创建教学班
  2. 从 `sc_course_qualification` 表查询教师资格
  3. 自动分配教师
  4. 保证合班上课时同一课程的多个行政班在同一时间
- 参数：提供 `course_class_mapping`
- 支持合班上课：同一课程可关联多个行政班

**教室类型自动匹配**：
- 后端会根据课程类型自动匹配合适的教室类型
- 通过 `sc_course_classroom_type` 关联表配置课程类型与教室类型的对应关系
- 无需前端手动指定教室类型

**响应**:
```json
{
  "code": 200,
  "message": "自动排课执行成功",
  "data": {
    "semester_uuid": "uuid-xxx",
    "schedule_map": {
      "1-1-2-[1,2,3]": [
        {
          "teaching_class_uuid": "uuid-xxx",
          "teaching_class_name": "高等数学-计科2101",
          "course_uuid": "course-xxx",
          "course_name": "高等数学",
          "course_total_hours": 64,
          "teacher_uuid": "teacher-xxx",
          "teacher_name": "张老师",
          "classroom_uuid": "classroom-xxx",
          "classroom_name": "A-101",
          "classroom_capacity": 60,
          "classroom_type_uuid": "type-xxx",
          "course_type_uuid": "type-xxx",
          "class_uuids": ["class-xxx"],
          "total_students": 45,
          "time_slot": {
            "day_of_week": 1,
            "section_start": 1,
            "section_end": 2,
            "weeks": [1, 2, 3]
          }
        }
      ]
    },
    "fitness": 8500.5,
    "hard_conflicts": 0,
    "soft_conflicts": 5,
    "unscheduled_teaching_classes": [],
    "conflict_report": {
      "hard_conflicts": [],
      "soft_conflicts": [...]
    },
    "statistics": {
      "total_teaching_classes": 10,
      "scheduled_teaching_classes": 10,
      "total_sessions": 60,
      "total_hours": 120,
      "average_fitness": 850.05
    },
    "is_success": true
  }
}
```

### 2. 保存预览方案

**接口路径**: `POST /v1/auto-schedule/save-preview`

**请求头**:
```
Authorization: Bearer {token}
```

**请求参数**:
- Query 参数: `semester_uuid` (string) - 学期UUID
- Request Body: `AutoScheduleResult` - 排课结果

**响应**:
```json
{
  "code": 200,
  "message": "保存预览成功",
  "data": null
}
```

### 3. 确认排课方案

**接口路径**: `POST /v1/auto-schedule/confirm`

**请求头**:
```
Authorization: Bearer {token}
```

**请求参数**:
- Query 参数: `semester_uuid` (string) - 学期UUID

**响应**:
```json
{
  "code": 200,
  "message": "确认排课方案成功",
  "data": null
}
```

### 4. 清除预览方案

**接口路径**: `DELETE /v1/auto-schedule/clear-preview`

**请求头**:
```
Authorization: Bearer {token}
```

**请求参数**:
- Query 参数: `semester_uuid` (string) - 学期UUID

**响应**:
```json
{
  "code": 200,
  "message": "清除预览成功",
  "data": null
}
```

### 5. 获取排课统计信息

**接口路径**: `GET /v1/auto-schedule/statistics`

**请求头**:
```
Authorization: Bearer {token}
```

**请求参数**:
- Query 参数: `semester_uuid` (string) - 学期UUID

**响应**:
```json
{
  "code": 200,
  "message": "获取统计信息成功",
  "data": {
    "total_teaching_classes": 10,
    "scheduled_teaching_classes": 10,
    "total_sessions": 60,
    "total_hours": 120,
    "average_fitness": 850.05
  }
}
```

## 相关接口

除了5个自动排课核心接口外，还需要以下接口支持：

- `GET /v1/semester/getPage` - 获取学期列表
- `GET /v1/course/getPage` - 获取课程列表
- `GET /v1/class/getPage` - 获取行政班列表
- `GET /v1/course-qualification/teachers` - 获取课程的教师列表（可选，用于展示）

## 请求参数说明

### AutoScheduleVO 参数

| 参数名 | 类型 | 必填 | 说明 | 默认值 |
|--------|------|------|------|--------|
| semester_uuid | string | 是 | 学期UUID | - |
| course_class_mapping | object | 是 | 课程-行政班映射 | - |
| teacher_assignment | object | 否 | 指定教师映射（课程UUID -> 教师UUID） | - |
| teacher_selection_strategy | string | 否 | 教师选择策略：balanced/random/first | balanced |
| building_uuids | string[] | 否 | 可用教学楼UUID列表 | 查询所有 |
| schedule_mode | number | 否 | 排课模式：0-预览, 1-正式 | 0 |
| overwrite | boolean | 否 | 是否覆盖已有排课记录 | false |
| population_size | number | 否 | 种群大小 | 100 |
| max_generations | number | 否 | 最大迭代代数 | 500 |
| crossover_rate | number | 否 | 交叉概率 | 0.8 |
| mutation_rate | number | 否 | 变异概率 | 0.2 |
| elite_size | number | 否 | 精英保留数量 | 10 |

> **注意**：教室类型由后端根据课程类型自动匹配，通过 `sc_course_classroom_type` 表配置，无需前端指定。

### AutoScheduleResult 响应说明

| 字段名 | 类型 | 说明 |
|--------|------|------|
| semester_uuid | string | 学期UUID |
| schedule_map | object | 排课结果映射，key为时间槽字符串，value为课程安排数组 |
| fitness | number | 适应度值 |
| hard_conflicts | number | 硬约束冲突数量 |
| soft_conflicts | number | 软约束冲突数量 |
| unscheduled_teaching_classes | string[] | 未排课的教学班UUID列表 |
| conflict_report | object | 冲突报告 |
| statistics | object | 统计信息 |
| is_success | boolean | 是否成功 |

### ScheduleStatistics 统计信息

| 字段名 | 类型 | 说明 |
|--------|------|------|
| total_teaching_classes | number | 总教学班数 |
| scheduled_teaching_classes | number | 已排课教学班数 |
| total_sessions | number | 总课时数 |
| total_hours | number | 总学时 |
| average_fitness | number | 平均适应度 |

### TimeSlot 时间槽

| 字段名 | 类型 | 说明 |
|--------|------|------|
| day_of_week | number | 星期几 (1-7) |
| section_start | number | 起始节次 (1,3,5,7,9,11) |
| section_end | number | 结束节次 (2,4,6,8,10,12) |
| weeks | number[] | 上课周次列表 |

### CourseAppointment 课程安排

| 字段名 | 类型 | 说明 |
|--------|------|------|
| teaching_class_uuid | string | 教学班UUID |
| teaching_class_name | string | 教学班名称 |
| course_uuid | string | 课程UUID |
| course_name | string | 课程名称 |
| course_total_hours | number | 课程总学时 |
| teacher_uuid | string | 教师UUID |
| teacher_name | string | 教师姓名 |
| classroom_uuid | string | 教室UUID |
| classroom_name | string | 教室名称 |
| classroom_capacity | number | 教室容量 |
| classroom_type_uuid | string | 教室类型UUID |
| course_type_uuid | string | 课程类型UUID |
| class_uuids | string[] | 行政班UUID列表 |
| total_students | number | 学生总人数 |
| time_slot | object | 时间槽信息 |

## 排课流程

### 按行政班级排课流程

```
用户选择学期
  ↓
加载课程列表
  ↓
用户选择课程
  ↓
加载该课程的行政班列表
  ↓
用户选择要排课的行政班（支持多选，用于合班上课）
  ↓
（可选）为课程指定教师或选择教师分配策略
  ↓
添加到课程-行政班映射中
  ↓
重复以上步骤添加更多课程
  ↓
（可选）配置资源范围（教学楼）
  ↓
（可选）配置算法参数
  ↓
选择排课模式（预览/正式）
  ↓
点击"开始排课"
  ↓
后端自动处理：匹配/创建教学班、分配教师、匹配教室类型、确保合班时间一致
  ↓
显示进度和日志
  ↓
算法完成，展示结果
  ↓
查看课表、冲突报告、统计信息
  ↓
根据结果选择操作：
  - 保存预览 → 保存为预览状态
  - 确认方案 → 将预览转为正式方案
  - 重新排课 → 调整参数后重试
```

### 预览模式流程

```
执行排课（schedule_mode=0）
  ↓
查看排课结果
  ↓
如满意 → 保存预览
  ↓
继续调整其他教学班
  ↓
所有排课满意 → 确认方案
  ↓
所有预览转为正式方案
```

### 正式模式流程

```
执行排课（schedule_mode=1）
  ↓
排课结果直接写入正式课表
  ↓
查看课表和统计
  ↓
如需调整：
  - 清除已有排课（overwrite=true）
  - 重新执行排课
```

## 排课优势

按行政班级排课模式有以下优势：

1. **简化流程**：无需提前创建教学班，直接从课程和行政班角度操作
2. **智能匹配**：自动复用已有教学班或创建新教学班
3. **自动分配**：自动选择有资格的教师，减少手动工作
4. **合班支持**：天然支持合班上课，确保同一课程的多个行政班在同一时间
5. **灵活配置**：支持指定特定教师或采用不同策略自动选择
6. **教室类型自动匹配**：根据课程类型自动选择合适类型的教室

## 关键文件路径

### 后端文件
- 控制器：`src/main/java/io/github/flashlack1314/smartschedulecorev2/controller/AutoScheduleController.java`
- 服务接口：`src/main/java/io/github/flashlack1314/smartschedulecorev2/service/AutoScheduleService.java`
- 服务实现：`src/main/java/io/github/flashlack1314/smartschedulecorev2/service/impl/AutoScheduleLogic.java`
- 请求VO：`src/main/java/io/github/flashlack1314/smartschedulecorev2/model/vo/AutoScheduleVO.java`
- 响应DTO：`src/main/java/io/github/flashlack1314/smartschedulecorev2/algorithm/dto/AutoScheduleResult.java`
